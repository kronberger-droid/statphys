#import "@preview/lilaq:0.5.0" as lq
#import "lib.typ": gauss

#let data = json("../data/P1_3.json")

#set page(flipped: true)

#align(center)[#text(size: 20pt)[Kronberger_P1_3b: Diffusion with reflecting walls]]

// TODO: plot all curves in a single diagram, one line per reduced time
